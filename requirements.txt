anthropic>=0.105.0
httpx
python-docx
pdfplumber
pypdf
faster-whisper
# torch: install with CUDA for GPU support (recommended):
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
# CPU-only fallback (slower transcription):
torch
fastapi
uvicorn[standard]
python-multipart
imageio-ffmpeg
pywebview>=5.0

# Testing
pytest>=7.0
pytest-asyncio>=0.23.0
reportlab
